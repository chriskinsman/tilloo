language: node_js
node_js:
  - 4.2.2


build:
  ci:
    - npm install gulp -g
    - npm install
    - bower --allow-root install
    - gulp lint
    - cd build-images
    - ./build-all.sh
  cache: true

